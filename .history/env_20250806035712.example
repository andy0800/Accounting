# MongoDB Atlas Connection String
# Replace with your actual MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/visa_system?retryWrites=true&w=majority

# Server Configuration
PORT=5000
NODE_ENV=production

# CORS Configuration (for production)
# Replace with your frontend domain
CORS_ORIGIN=https://your-frontend-domain.railway.app

# Optional: JWT Secret (if you add authentication later)
# JWT_SECRET=your-super-secret-jwt-key

# Optional: Email Configuration (if you add email features later)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password 